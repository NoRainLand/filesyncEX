# 旧工程（保留参考，不纳入新仓库）
filesync

# 依赖
node_modules/

# 本地 pnpm store（仓库内 store-dir 时产生）
.pnpm-store/

# 构建产物（各包 tsc / vite / esbuild 输出）
dist/
**/dist/

# 打包产物
release/

# pnpm patch 临时目录（patches/ 补丁文件本身保留提交）
.pnpm_patches/

# 运行时数据（上传文件 / 数据库 / 实例锁）
data/
**/data/

# 日志与临时输出
*.log
*.err
server.log
server.err
bt.log
bt.err
.tmp/

# 附加工具（QuickSendTool 已改为 GitHub releases 分发，不再内置）

# 测试临时脚本 / 临时目录（正式测试已迁到 packages/server/test/，随仓库提交）
*.tmp
tmp_log_append.md
.exetest/
.bt/

# 本地验证临时文件 —— 统一放根目录 _dev/（启动脚本 / 内存数据 / 临时截图等一律写这里，勿散落到各处）
_dev/

# 系统 / 编辑器
.DS_Store
Thumbs.db

# 测试文件夹
TestRes